<!DOCTYPE html>
<html>
<head>
  <title>Auction</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
  
  <div class="container">
    <%= render 'layouts/header' %>
    <div class="row">
      <div class="col-md-5"><h1><%= @current_auction %></h1></div>
<!-- TODO Implement conditional start/finish countdowns and provide dates dynamically. -->
      <div class="col-md-7 h3 countdown"><%= countdown_explanation %>
        <div id="getting-started"></div>
        <%= javascript_tag do %>
          $("#getting-started")
            .countdown(<%= config_countdown %>, function(event) {
            $(this).text(
              event.strftime('%D Days %H:%M:%S')
            );
          });
        <% end %>
      </div>
    </div>
    <h2>May 16th - 18th, 2015</h2>
<!--     TODO: edit hardcoded auction dates for layout -->
  </div>
  
<body>

<div class="container">
  <%= render 'layouts/flash' %>
  <%= yield %>
</div>

</body>
</html>
