<h1>Items Up for Bid</h1>
<table>
  <thead>
    <tr>
      <th>Item</th>
      <th>Current Bid</th>
    </tr>
  </thead>
  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= link_to item.name, item_path(item) %></td>
        <td><%= item.bids.last.amount %></td>
      </tr>
    <% end %>
  </tbody>
</table>






